import React from "react";
import PropTypes from "prop-types";

NoMatch.propTypes = {};

function NoMatch(props) {
  return (
    <div style={{ fontSize: "50px", marginTop: "200px", textAlign: "center" }}>
      404
    </div>
  );
}

export default NoMatch;
